{
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --gc --minify && bunx pagefind --site public"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "mathjax": "^4.1.0",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}